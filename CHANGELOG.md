# 更新日志

本文件记录每个发布版本的变化。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.3] - 2026-09-07

### 新增

- 自建 ddddocr 服务现在同时加载 **default 和 beta 两个模型**，可在设置里切换。两个模型是独立训练的，恰恰在难认字形上会给出不同答案（例如被干扰线穿过的 `v` 认成 `y`），换一个试往往比继续调预处理有效
- **把字符集一并发给引擎**，由模型在指定字母表内选最优解；此前是事后把越界字符删掉，会把「认错一个字」变成「少一个字」

## [1.1.2] - 2026-09-07

### 修复

- **自建 HTTP 引擎改为发送原始图片**，不再先做灰度化。ddddocr 这类模型是拿原始验证码图训练的，会自己处理背景和干扰线；先灰度化反而抹掉它依赖的颜色信息 —— 粉底绿字转灰度后字就和背景糊在一起，字符直接消失。需要干净输入的接口可在设置里单独开启预处理
- **不再为自建引擎编造置信度**。之前固定报 90%，等于在错误答案旁边放一个让人放心的数字；现在如实显示「不适用」，并改用长度校验作为判定依据
- 识别测试里那张图的标题改为「实际发送（未预处理）」，不再在没预处理时仍标注「预处理后」

## [1.1.1] - 2026-09-07

### 修复

- 自建 HTTP 识别服务连不上时，**自动退回内置引擎**，不再整个功能失效
- 不再把浏览器的 `Failed to fetch` 原样甩给用户，改为「连不上识别服务 —— 服务没启动？双击 start-ocr.cmd」这类可操作的提示
- 设置 → 图片验证码 → 自建 HTTP 接口 新增 **测试接口** 按钮，往接口发一张已知内容的图并核对结果

## [1.1.0] - 2026-09-02

### 新增

- **自动填写手机号**：在 设置 → 常规 → 自动填写手机号 里填上自己的号码，遇到「手机号 / 电话 / mobile / `type="tel"`」这类输入框时自动填入
  - 留空则整个功能完全不生效
  - 验证码框、密码、邮箱、搜索框、身份证、银行卡一律排除
  - 默认不覆盖已有内容；手动清空后不会被再次填回
  - 号码明文保存在 `chrome.storage.local`（与只存内存的验证码不同）

### 修复

- 仅凭 `placeholder="请输入验证码"` 也能识别验证码框：附近没有图片时，这个标签只可能指短信验证码
- 手动指定输入框的取词模式现在在所有 frame 生效，登录表单在 iframe 里也能指定
- 图像预处理的去噪改到放大之前执行 —— 之前放大后再去噪等于没做
- 识别不出内容时也会返回预处理图，方便排查

## [1.0.0] - 2026-09-02

首个版本。

### 短信验证码

- 打分式短信解析：按关键词距离、长度、上下文给每个候选打分，能排除手机号、订单号、`5分钟`、`300元` 这类干扰数字
- 四种来源：WebSocket 推送、HTTP 轮询、剪贴板、手动粘贴
- 零依赖本地桥接服务（`bridge/server.mjs`，含手写 WebSocket 实现），令牌鉴权，接受 JSON / 表单 / 纯文本三种 webhook 格式
- 双击即用的启动脚本（`start-bridge.cmd` / `start-bridge.sh`），自动定位项目路径
- 一键生成桌面快捷方式（`create-shortcut.cmd` / `npm run shortcut`），Windows / macOS / Linux 三平台
- 令牌持久化保存，每次启动不变，扩展里只需填写一次

### 图片验证码

- 内置 Tesseract WASM 离线 OCR，图片不出本机
- 取像素时不重新请求图片 URL，避免作废用户眼前的那张验证码
- 预处理：裁边、放大、灰度、大津二值化、去噪，可逐项调整
- 多方案择优（正常 / 反色 / 纯灰度），按置信度与期望长度评分
- 可切换为自建 HTTP 识别接口；仓库自带开箱即用的 ddddocr 服务（`start-ocr.cmd`），应对扭曲、干扰线、彩色噪点这类 Tesseract 无解的验证码

### 页面适配

- 手动指定验证码输入框：自动识别失败时，在弹窗里点一下就能指定，按站点记住；取词模式在所有 frame 生效，表单在 iframe 里也能指定
- 旁边有「获取验证码」按钮时，即使字段毫无命名线索也能识别（并排除同样靠近按钮的手机号框）
- 仅凭 `placeholder="请输入验证码"` 也能识别：附近没有图片时，这个标签只可能指短信验证码

- 区分「短信验证码框」与「图片验证码框」：命名只投票，由相邻图片或「获取验证码」按钮裁决
- 支持分格输入框、Shadow DOM、iframe
- 拟真键盘输入，React / Vue 受控组件可正常响应
- 安装、更新、浏览器启动时自动注入已打开的标签页

### 安全与隐私

- 验证码只写入 `chrome.storage.session`（纯内存），浏览器关闭即清除
- 自动提交默认关闭
- 站点黑名单 / 白名单
- OCR 频率限制与换图重试上限
